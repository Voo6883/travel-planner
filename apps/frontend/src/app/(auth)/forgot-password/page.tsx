import { ForgotPasswordPanel } from '@/features/auth';

/** `/forgot-password` — UC-A07. `POST /auth/password/forgot`. */
export default function ForgotPasswordPage() {
  return <ForgotPasswordPanel />;
}
