spring:
  application:
    name: travel-planner-backend
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
