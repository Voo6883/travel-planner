/**
 * Pure domain model — entities, value objects, and port interfaces.
 *
 * <p>Nothing here may import Spring, JPA, LangChain4j, or any vendor SDK (PLAN §4.0.1).
 * Intentionally empty in task 02: the layer boundary is established before any business type
 * exists, so ArchUnit rules in tasks/15-quality-gates.md have something to enforce.
 */
package com.travelplanner.domain;
