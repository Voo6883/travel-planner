/**
 * Spring configuration classes and typed configuration properties.
 *
 * <p>Wiring only — no business rules. Empty in task 02; security, datasource, and AI provider
 * configuration arrive with their owning tasks.
 */
package com.travelplanner.config;
