/**
 * AI layer — agents, tools, prompts, and the LangChain4j adapter.
 *
 * <p>LangChain4j types may not appear outside {@code ai.langchain4j} (AGENTS.md). Populated by
 * tasks/14-ai-provider-platform.md; empty here so the boundary exists from the start.
 */
package com.travelplanner.ai;
